ICC/*.icc
Results/*.icc
Results/*.tif
Results/cmykGrays*.txt
config/*.json

# Every other config here is generated by BuildAndTest via -exportcfg, but the
# spectral image reproduction steps are driven by connect.useSearch, which is
# -cfg only: the positional form has no room for pccWeights or the
# reverse-search start.  These two are source, not output, and are tracked the
# way the ICS packages track their scenario configs.
!config/msCowsIconToCmyk.json
!config/msCowsToCmyk.json

